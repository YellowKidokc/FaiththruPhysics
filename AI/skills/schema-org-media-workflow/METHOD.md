'# Method

1. Run manifest pass to confirm status buckets (`already in R2`, `found locally`, `missing`, `review`).
2. Keep canonical file prefixes (`1D_`, `2DS_`, `3C_`, `4V_`) as primary type source.
3. Emit JSON-LD with conservative fallback and local-source tags.
4. Attach schema metadata at folder or page scope according to resolved inheritance.
5. Scope is locked to `D:\GitHub\faiththruphysics-site-data`; no other DataRoot allowed.

Use only for confirmed mappings.
'
