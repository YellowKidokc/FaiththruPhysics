# Expandable Header (ftp-shell-v2.5.1 Layer 1 + Layer 2)
#
# The canonical expandable "Verification & Proof" header lives here.
# Source: Python-WEB/topbar/canonical-page-shell (ftp-panel-toggle / ftpPanel).
#
# Apply across article pages:
#   python scripts/apply_expandable_header.py --path revolution-of-truth
#   python scripts/apply_expandable_header.py --path revolution-of-truth --apply
#   python scripts/apply_expandable_header.py --path . --apply
#
# Assets:
#   expandable-header.html  — topbar + panel toggle + expandable panel markup
#   shell.css               — styles for the header/panel
#   shell.js                — runtime (ftp.togglePanel, tabs, shell-data)
#   canonical-page-shell/   — full v2.5.1 assemble/verify toolkit
#   universal-shell/        — Kimi universal shell reference + INTEGRATION-GUIDE
